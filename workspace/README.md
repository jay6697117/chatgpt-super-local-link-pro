# Workspace

默认 ROOTS 指向这个目录。请在 `.env` 中把 `ROOTS` 改成你的真实项目路径，例如：

```env
ROOTS=D:/work/JiaDianAI;D:/mcp-work/harmonyos-libretro-emulator
```

第一次连接 ChatGPT 时建议保持：

```env
ALLOW_WRITE=0
ALLOW_OVERWRITE=0
```
